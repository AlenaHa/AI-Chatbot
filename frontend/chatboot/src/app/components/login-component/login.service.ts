import { Injectable } from '@angular/core';
import { Http, ResponseContentType, Headers } from '@angular/http';

@Injectable()

export class LoginService{
    /* aici vom face requesturile la endpointuri din API*/
    /* sau vom scrie alte servicii care ajuta in cadrul componenentei
        ex : validare intr-un anumit fel a unui input*/
}